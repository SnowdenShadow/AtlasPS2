/*
 * AtlasPS2 - tests/host/io_common.h
 *
 * The PS2SDK header of this name carries the FIO_* flags. The host
 * stand-in for fileXio_rpc.h defines the one flag image.c uses, so this
 * exists only so the include line in the module under test resolves
 * without being edited for the check.
 */
#ifndef ATLAS_HOST_IO_COMMON_H
#define ATLAS_HOST_IO_COMMON_H

#endif /* ATLAS_HOST_IO_COMMON_H */
